<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WatchParadiseAdmin" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/theme_color_main</item>
    </style>

    <style name="Theme.WatchParadiseAdmin" parent="Base.Theme.WatchParadiseAdmin" />

    <style name="Theme.WatchParadise.FullScreen" parent="Theme.WatchParadiseAdmin">
        <item name="backgroundColor">@color/theme_color_body</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>